# 🚀 Руководство по деплою

Это пошаговое руководство поможет вам задеплоить проект на продакшен.

## 📋 Предварительные требования

1. **PostgreSQL база данных** - создайте базу данных на одном из сервисов:
   - [Vercel Postgres](https://vercel.com/docs/storage/vercel-postgres)
   - [Railway](https://railway.app/)
   - [Render](https://render.com/)
   - [Supabase](https://supabase.com/)
   - [Neon](https://neon.tech/)

2. **Telegram Bot Token** - получите у [@BotFather](https://t.me/botfather)

3. **Robokassa аккаунт** - зарегистрируйтесь и создайте магазин:
   - MerchantLogin (логин магазина)
   - Password #1 (для подписи платежей)
   - Password #2 (для проверки webhook)

## 🔧 Шаг 1: Подготовка базы данных

### 1.1 Создайте PostgreSQL базу данных

Выберите один из вариантов:

**Vercel Postgres:**
```bash
# В Vercel Dashboard: Storage → Create Database → Postgres
# Скопируйте DATABASE_URL из настроек
```

**Railway:**
```bash
# Создайте новый проект → Add Database → PostgreSQL
# Скопируйте DATABASE_URL из переменных окружения
```

**Render:**
```bash
# New → PostgreSQL → Create Database
# Скопируйте Internal Database URL
```

### 1.2 Примените миграции

После получения `DATABASE_URL`, примените миграции:

```bash
# Установите DATABASE_URL
export DATABASE_URL="postgresql://user:password@host:5432/dbname"

# Примените миграции
pnpm db:migrate:deploy

# (Опционально) Заполните базу тестовыми данными
pnpm db:seed
```

## 🌐 Шаг 2: Выбор платформы для деплоя

### Вариант A: Vercel (Рекомендуется для Next.js)

1. **Подключите репозиторий:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Import Project → выберите ваш репозиторий

2. **Настройте переменные окружения:**
   В Settings → Environment Variables добавьте:
   ```
   TG_BOT_TOKEN=your_bot_token
   ROBOKASSA_MERCHANT_LOGIN=your_merchant_login
   ROBOKASSA_PASSWORD1=your_password1
   ROBOKASSA_PASSWORD2=your_password2
   APP_BASE_URL=https://your-app.vercel.app
   DATABASE_URL=postgresql://... (из Vercel Postgres)
   ADMIN_KEY=your_secret_key
   FULFILLMENT_PROVIDER=mock
   ROBOKASSA_TEST_MODE=false
   ```

3. **Настройте Build Command:**
   ```
   pnpm install && pnpm prisma generate && pnpm db:migrate:deploy && pnpm build
   ```

4. **Деплой:**
   - Vercel автоматически задеплоит при push в main
   - Или нажмите "Deploy" вручную

### Вариант B: Railway

1. **Создайте проект:**
   - Зайдите на [railway.app](https://railway.app)
   - New Project → Deploy from GitHub repo

2. **Добавьте PostgreSQL:**
   - New → Database → PostgreSQL
   - Railway автоматически создаст `DATABASE_URL`

3. **Настройте переменные окружения:**
   В Variables добавьте все переменные из `env.example`

4. **Настройте Build Command:**
   ```
   pnpm install && pnpm prisma generate && pnpm db:migrate:deploy && pnpm build
   ```

### Вариант C: Render

1. **Создайте Web Service:**
   - New → Web Service → подключите репозиторий

2. **Настройте:**
   - Build Command: `pnpm install && pnpm prisma generate && pnpm db:migrate:deploy && pnpm build`
   - Start Command: `pnpm start`

3. **Добавьте PostgreSQL:**
   - New → PostgreSQL
   - Скопируйте Internal Database URL в переменные окружения

4. **Настройте переменные окружения:**
   Добавьте все переменные из `env.example`

## 🔐 Шаг 3: Настройка переменных окружения

Скопируйте все переменные из `env.example` и заполните реальными значениями:

```env
# Обязательные
TG_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TBANK_TERMINAL_KEY=1234567890123
TBANK_PASSWORD=your_tbank_password
APP_BASE_URL=https://your-app.vercel.app
DATABASE_URL=postgresql://user:pass@host:5432/db
ADMIN_KEY=your_very_secret_admin_key_here

# Опциональные
FULFILLMENT_PROVIDER=mock
NEXT_PUBLIC_SUPPORT_BOT_USERNAME=support
```

## 🔗 Шаг 4: Настройка Robokassa Webhook

После деплоя настройте webhook в Robokassa:

1. Зайдите в личный кабинет Robokassa
2. Настройки магазина → Технические настройки
3. Укажите **Result URL**: `https://your-domain.com/api/payments/robokassa/notification`
4. (Опционально) Укажите **Success URL** и **Fail URL**
5. Сохраните настройки

## ✅ Шаг 5: Проверка после деплоя

### 5.1 Проверьте работу страниц:
- ✅ `/catalog` - каталог товаров
- ✅ `/cart` - корзина
- ✅ `/checkout` - оформление заказа
- ✅ `/orders/[id]` - статус заказа

### 5.2 Протестируйте создание заказа:
1. Откройте приложение в Telegram
2. Добавьте товары в корзину
3. Оформите заказ
4. Проверьте, что открывается страница оплаты Robokassa

### 5.3 Проверьте webhook:
1. Сделайте тестовый платеж (используйте `ROBOKASSA_TEST_MODE=true` для тестирования)
2. Проверьте логи на хостинге
3. Убедитесь, что статус заказа обновляется на PAID после оплаты

### 5.4 Проверьте fulfillment:
1. После успешной оплаты проверьте логи
2. Убедитесь, что fulfillment запускается
3. Проверьте статус заказа в базе данных

## 🐛 Решение проблем

### Ошибка: "DATABASE_URL is not configured"
- Убедитесь, что переменная `DATABASE_URL` установлена на хостинге
- Проверьте формат: `postgresql://user:password@host:5432/dbname`

### Ошибка: "Prisma migrate deploy failed"
- Убедитесь, что база данных доступна
- Проверьте права доступа к базе данных
- Попробуйте `pnpm db:push` вместо `migrate deploy` (только для dev)

### Ошибка: "TG_BOT_TOKEN is not configured"
- Проверьте, что токен бота установлен в переменных окружения
- Убедитесь, что токен правильный (получите новый у @BotFather)

### Ошибка: "ROBOKASSA_MERCHANT_LOGIN or ROBOKASSA_PASSWORD1 is not configured"
- Проверьте, что все переменные Robokassa установлены
- Убедитесь, что используете правильные пароли (Password1 и Password2 разные!)

### Webhook не работает
- Проверьте, что `APP_BASE_URL` правильный и использует HTTPS
- Убедитесь, что Result URL настроен в Robokassa
- Проверьте, что `ROBOKASSA_PASSWORD2` правильный (используется для проверки подписи)
- Проверьте логи на хостинге для ошибок
- Robokassa ожидает ответ "OK" в случае успешной обработки

## 📚 Полезные команды

```bash
# Локальная разработка
pnpm dev                    # Запуск dev сервера
pnpm dev:https             # Запуск с HTTPS (для Telegram)

# База данных
pnpm db:migrate            # Создать и применить миграцию (dev)
pnpm db:migrate:deploy     # Применить миграции (production)
pnpm db:push               # Применить схему без миграций
pnpm db:seed               # Заполнить базу тестовыми данными
pnpm db:studio             # Открыть Prisma Studio

# Сборка и тесты
pnpm build                 # Собрать проект
pnpm test:all              # Запустить все тесты
```

## 🔄 Обновление после деплоя

1. **Добавили новую миграцию?**
   ```bash
   pnpm db:migrate:deploy
   ```

2. **Изменили переменные окружения?**
   - Обновите их на хостинге
   - Перезапустите приложение

3. **Обновили код?**
   - Push в main ветку (для Vercel/Railway)
   - Или запустите деплой вручную

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи на хостинге
2. Проверьте переменные окружения
3. Убедитесь, что база данных доступна
4. Проверьте `DEPLOY_CHECKLIST.md` для дополнительной информации


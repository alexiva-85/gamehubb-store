# 🚀 Инструкция по деплою проекта

Пошаговая инструкция для деплоя Telegram Mini App магазина.

## 📋 Подготовка

### 1. Создайте аккаунты и получите доступы

**Robokassa:**
- Зарегистрируйтесь на [robokassa.com](https://robokassa.com/)
- Создайте магазин
- Получите:
  - `MerchantLogin` (логин магазина)
  - `Password #1` (для подписи платежей)
  - `Password #2` (для проверки webhook)

**Telegram Bot:**
- Создайте бота через [@BotFather](https://t.me/botfather)
- Получите `TG_BOT_TOKEN`

**PostgreSQL база данных:**
- Выберите один из сервисов:
  - [Vercel Postgres](https://vercel.com/docs/storage/vercel-postgres) (бесплатно до 256MB)
  - [Railway](https://railway.app/) (бесплатно $5 кредитов)
  - [Render](https://render.com/) (бесплатно до 90 дней)
  - [Supabase](https://supabase.com/) (бесплатно до 500MB)
  - [Neon](https://neon.tech/) (бесплатно до 0.5GB)

## 🌐 Вариант 1: Деплой на Vercel (Рекомендуется)

### Шаг 1: Подготовка репозитория

1. Создайте репозиторий на GitHub/GitLab/Bitbucket
2. Загрузите код:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <ваш-репозиторий-url>
   git push -u origin main
   ```

### Шаг 2: Создание проекта в Vercel

1. Зайдите на [vercel.com](https://vercel.com) и войдите через GitHub
2. Нажмите **"Add New..." → "Project"**
3. Импортируйте ваш репозиторий
4. Настройте проект:
   - **Framework Preset:** Next.js
   - **Root Directory:** `./` (оставьте как есть)
   - **Build Command:** `pnpm install && pnpm prisma generate && pnpm db:migrate:deploy && pnpm build`
   - **Output Directory:** `.next` (по умолчанию)
   - **Install Command:** `pnpm install`

### Шаг 3: Настройка базы данных

**Вариант A: Vercel Postgres (проще всего)**
1. В Vercel Dashboard: **Storage → Create Database → Postgres**
2. Выберите регион (ближайший к вам)
3. Создайте базу данных
4. Vercel автоматически создаст переменную `POSTGRES_URL`
5. Скопируйте `POSTGRES_URL` - это ваш `DATABASE_URL`

**Вариант B: Внешняя PostgreSQL**
1. Создайте базу на другом сервисе (Railway, Render и т.д.)
2. Скопируйте connection string (например: `postgresql://user:pass@host:5432/dbname`)

### Шаг 4: Настройка переменных окружения

В Vercel Dashboard: **Settings → Environment Variables**

Добавьте все переменные:

```env
# Telegram
TG_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Robokassa
ROBOKASSA_MERCHANT_LOGIN=your_merchant_login
ROBOKASSA_PASSWORD1=your_password1
ROBOKASSA_PASSWORD2=your_password2
ROBOKASSA_TEST_MODE=false

# База данных (если используете Vercel Postgres, используйте POSTGRES_URL)
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Приложение
APP_BASE_URL=https://your-app.vercel.app
ADMIN_KEY=your_very_secret_admin_key_here

# Fulfillment (опционально)
FULFILLMENT_PROVIDER=mock
```

**Важно:**
- `APP_BASE_URL` должен быть HTTPS URL вашего приложения
- После первого деплоя Vercel даст вам домен, обновите `APP_BASE_URL`
- `ADMIN_KEY` - придумайте сложный случайный ключ (минимум 32 символа)

### Шаг 5: Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 2-5 минут)
3. После успешного деплоя скопируйте URL приложения (например: `https://your-app.vercel.app`)

### Шаг 6: Применение миграций базы данных

После первого деплоя нужно применить миграции:

**Вариант A: Через Vercel CLI**
```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Примените миграции
vercel env pull .env.local
pnpm prisma migrate deploy
```

**Вариант B: Через Vercel Dashboard**
1. Зайдите в **Settings → Environment Variables**
2. Убедитесь, что `DATABASE_URL` установлен
3. В разделе **Deployments** найдите последний деплой
4. Откройте **Functions → View Function Logs**
5. Или используйте Vercel CLI для выполнения команд

**Вариант C: Локально (если DATABASE_URL доступен)**
```bash
# Установите DATABASE_URL локально
export DATABASE_URL="postgresql://..."

# Примените миграции
pnpm prisma migrate deploy

# (Опционально) Заполните тестовыми данными
pnpm prisma db seed
```

### Шаг 7: Настройка Robokassa

1. Зайдите в личный кабинет Robokassa
2. Настройки магазина → **Технические настройки**
3. Укажите **Result URL** (webhook):
   ```
   https://your-app.vercel.app/api/payments/robokassa/notification
   ```
4. (Опционально) Укажите **Success URL** и **Fail URL**:
   ```
   https://your-app.vercel.app/pay/success?orderId={orderId}
   https://your-app.vercel.app/pay/fail?orderId={orderId}
   ```
5. Сохраните настройки

### Шаг 8: Настройка Telegram Bot

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. **Bot Settings → Menu Button**
5. Укажите URL вашего приложения:
   ```
   https://your-app.vercel.app
   ```

## 🌐 Вариант 2: Деплой на Railway

### Шаг 1: Создание проекта

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. **New Project → Deploy from GitHub repo**
4. Выберите ваш репозиторий

### Шаг 2: Добавление PostgreSQL

1. **New → Database → PostgreSQL**
2. Railway автоматически создаст переменную `DATABASE_URL`
3. Скопируйте `DATABASE_URL` из настроек базы данных

### Шаг 3: Настройка переменных окружения

В настройках проекта: **Variables**

Добавьте все переменные (см. список выше)

### Шаг 4: Настройка Build Command

В настройках проекта: **Settings → Deploy**

Установите:
- **Build Command:** `pnpm install && pnpm prisma generate && pnpm db:migrate:deploy && pnpm build`
- **Start Command:** `pnpm start`

### Шаг 5: Деплой

Railway автоматически задеплоит при push в main ветку.

### Шаг 6: Применение миграций

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите
railway login

# Подключитесь к проекту
railway link

# Примените миграции
railway run pnpm prisma migrate deploy
```

### Шаг 7: Настройка Robokassa и Telegram

См. шаги 7-8 из варианта 1 (Vercel), заменив URL на ваш Railway домен.

## 🌐 Вариант 3: Деплой на Render

### Шаг 1: Создание Web Service

1. Зайдите на [render.com](https://render.com)
2. **New → Web Service**
3. Подключите ваш репозиторий

### Шаг 2: Настройка

- **Name:** ваш-магазин
- **Environment:** Node
- **Build Command:** `pnpm install && pnpm prisma generate && pnpm db:migrate:deploy && pnpm build`
- **Start Command:** `pnpm start`

### Шаг 3: Добавление PostgreSQL

1. **New → PostgreSQL**
2. Скопируйте **Internal Database URL**
3. Добавьте его как `DATABASE_URL` в переменные окружения

### Шаг 4: Переменные окружения

Добавьте все переменные (см. список выше)

### Шаг 5: Деплой

Render автоматически задеплоит проект.

## ✅ Проверка после деплоя

### 1. Проверьте работу страниц

Откройте в браузере:
- `https://your-app.vercel.app/catalog` - каталог товаров
- `https://your-app.vercel.app/cart` - корзина
- `https://your-app.vercel.app/oferta` - публичная оферта

### 2. Проверьте в Telegram

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку меню (если настроена)
3. Проверьте, что приложение открывается

### 3. Протестируйте создание заказа

1. Добавьте товары в корзину
2. Оформите заказ
3. Проверьте, что открывается страница оплаты Robokassa

### 4. Проверьте webhook

1. Сделайте тестовый платеж (используйте `ROBOKASSA_TEST_MODE=true`)
2. Проверьте логи на хостинге
3. Убедитесь, что статус заказа обновляется на PAID

## 🔧 Полезные команды

```bash
# Локальная разработка
pnpm dev                    # Запуск dev сервера
pnpm dev:https             # Запуск с HTTPS (для Telegram)

# База данных
pnpm db:migrate            # Создать и применить миграцию (dev)
pnpm db:migrate:deploy     # Применить миграции (production)
pnpm db:push               # Применить схему без миграций
pnpm db:seed               # Заполнить базу тестовыми данными
pnpm db:studio             # Открыть Prisma Studio

# Сборка и тесты
pnpm build                 # Собрать проект
pnpm test:all              # Запустить все тесты
```

## 🐛 Решение проблем

### Ошибка: "DATABASE_URL is not configured"
- Убедитесь, что переменная `DATABASE_URL` установлена на хостинге
- Проверьте формат: `postgresql://user:password@host:5432/dbname`

### Ошибка: "Prisma migrate deploy failed"
- Убедитесь, что база данных доступна
- Проверьте права доступа к базе данных
- Попробуйте `pnpm db:push` вместо `migrate deploy` (только для dev)

### Ошибка: "TG_BOT_TOKEN is not configured"
- Проверьте, что токен бота установлен в переменных окружения
- Убедитесь, что токен правильный (получите новый у @BotFather)

### Webhook не работает
- Проверьте, что `APP_BASE_URL` правильный и использует HTTPS
- Убедитесь, что Result URL настроен в Robokassa
- Проверьте, что `ROBOKASSA_PASSWORD2` правильный
- Проверьте логи на хостинге для ошибок

### Приложение не открывается в Telegram
- Убедитесь, что URL использует HTTPS
- Проверьте, что URL настроен в @BotFather
- Попробуйте открыть URL в браузере - должно работать

## 📞 Дополнительная помощь

- **Документация проекта:** `README.md`
- **Чеклист деплоя:** `DEPLOY_CHECKLIST.md`
- **Миграция на Robokassa:** `ROBOKASSA_MIGRATION.md`
- **Подробное руководство:** `DEPLOY_GUIDE.md`

## 🎉 Готово!

После выполнения всех шагов ваш магазин будет доступен в Telegram и готов принимать заказы!



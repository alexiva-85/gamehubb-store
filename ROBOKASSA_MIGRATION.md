# Миграция с T-Bank на Robokassa

Интеграция с Robokassa успешно реализована. Этот документ описывает изменения и инструкции по настройке.

## ✅ Что было сделано

1. **Создан модуль `src/lib/robokassa.ts`**:
   - Функция `initPayment()` - формирует URL для оплаты
   - Функция `buildPaymentSignature()` - создает подпись для запроса
   - Функция `verifyNotificationSignature()` - проверяет подпись webhook
   - Функция `parseNotificationParams()` - парсит параметры уведомления

2. **Создан API route `src/app/api/payments/robokassa/notification/route.ts`**:
   - Обрабатывает webhook уведомления от Robokassa
   - Проверяет подпись уведомления
   - Обновляет статус заказа на PAID
   - Запускает fulfillment процесс

3. **Обновлен `src/app/api/orders/route.ts`**:
   - Использует Robokassa вместо T-Bank
   - Provider изменен с 'TBANK' на 'ROBOKASSA'

4. **Обновлена схема базы данных**:
   - Дефолтное значение `provider` изменено на 'ROBOKASSA'

5. **Обновлены переменные окружения**:
   - Заменены `TBANK_*` на `ROBOKASSA_*`

## 🔧 Настройка Robokassa

### 1. Регистрация в Robokassa

1. Зарегистрируйтесь на [Robokassa](https://robokassa.com/)
2. Создайте магазин в личном кабинете
3. Получите следующие данные:
   - **MerchantLogin** - логин магазина
   - **Password #1** - пароль для формирования подписи платежа
   - **Password #2** - пароль для проверки подписи уведомлений

### 2. Настройка переменных окружения

Добавьте в `.env` файл:

```env
# Robokassa настройки
ROBOKASSA_MERCHANT_LOGIN=your_merchant_login
ROBOKASSA_PASSWORD1=your_password1
ROBOKASSA_PASSWORD2=your_password2

# Опционально
ROBOKASSA_TEST_MODE=true  # Для тестового режима
ROBOKASSA_API_BASE_URL=https://auth.robokassa.ru/Merchant/Index.aspx
```

### 3. Настройка в личном кабинете Robokassa

1. Зайдите в настройки магазина
2. Укажите **Result URL** (webhook для уведомлений):
   ```
   https://your-domain.com/api/payments/robokassa/notification
   ```
3. (Опционально) Укажите **Success URL** и **Fail URL**:
   ```
   https://your-domain.com/pay/success?orderId={orderId}
   https://your-domain.com/pay/fail?orderId={orderId}
   ```
   Примечание: Эти URL также передаются в параметрах запроса, но можно настроить глобально в кабинете.

## 🔄 Отличия от T-Bank

### Формат платежа

**T-Bank:**
- Использует JSON API (`POST /v2/Init`)
- Возвращает `PaymentId` и `PaymentURL` в ответе
- Статусы: `NEW`, `AUTHORIZED`, `CONFIRMED`

**Robokassa:**
- Использует GET запрос с параметрами в URL
- Формирует URL напрямую (без API запроса)
- Отправляет уведомление только об успешной оплате

### Подпись

**T-Bank:**
- SHA256 хеш всех параметров + пароль
- Сложная логика фильтрации и сортировки полей

**Robokassa:**
- MD5 хеш: `MD5(MerchantLogin:OutSum:InvId:Password1)` для платежа
- MD5 хеш: `MD5(OutSum:InvId:Password2)` для проверки webhook
- Более простая логика

### Webhook

**T-Bank:**
- JSON формат
- Множественные статусы (AUTHORIZED, CONFIRMED)
- Может отправлять несколько уведомлений

**Robokassa:**
- `application/x-www-form-urlencoded` формат
- Только одно уведомление об успешной оплате
- Проще в обработке

## 📝 Миграция существующих данных

Если у вас есть заказы со статусом `provider = 'TBANK'`, они продолжат работать. Новые заказы будут создаваться с `provider = 'ROBOKASSA'`.

Для полной миграции можно:
1. Оставить старый webhook endpoint (`/api/payments/tbank/notification`) для обратной совместимости
2. Или удалить его после миграции всех старых заказов

## 🧪 Тестирование

1. Установите `ROBOKASSA_TEST_MODE=true` в `.env`
2. Используйте тестовые данные из личного кабинета Robokassa
3. Создайте тестовый заказ
4. Проверьте, что:
   - URL платежа формируется корректно
   - После оплаты приходит webhook
   - Статус заказа обновляется на PAID
   - Fulfillment запускается автоматически

## 📚 Дополнительная документация

- [Официальная документация Robokassa](https://docs.robokassa.ru/)
- [API документация](https://auth.robokassa.ru/Merchant/WebService/Service.asmx)

## ⚠️ Важные замечания

1. **Сумма платежа**: Robokassa принимает сумму в рублях (не копейки), конвертация происходит автоматически
2. **Подпись**: Всегда используйте верхний регистр для MD5 хеша
3. **Webhook**: Robokassa ожидает ответ "OK" в случае успешной обработки
4. **Тестовый режим**: Используйте `ROBOKASSA_TEST_MODE=true` для тестирования без реальных платежей

